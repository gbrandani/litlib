Manual Resolution Queue
Library root: /Users/brandani/Dropbox/documents/library
Status: all article PDFs currently have indexed metadata entries.
Notes:
- Supplements that should not carry standalone bibliographic entries are linked with `matched_supplement`.
- A small number of records were completed as `manual_verified` because the local filename did not match the actual paper or because a non-journal item required local metadata.
- For long-term maintenance, new unresolved additions should be appended here only when deterministic matching fails.
